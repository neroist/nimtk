import
  ./widgets/button,
  ./widgets/frame,
  ./widgets/label,
  ./widgets/labelframe,
  ./widgets/message,
  ./widgets/root,
  ./widgets/toplevel,
  ./widgets/checkbutton,
  ./widgets/radiobutton,
  ./widgets/scale,
  ./widgets/entry,
  ./widgets/listbox,
  ./widgets/spinbox,
  ./widgets/scrollbar,
  ./widgets/widget

export
  button,
  frame,
  label,
  labelframe,
  message,
  root,
  toplevel,
  checkbutton,
  radiobutton,
  scale,
  entry,
  listbox,
  spinbox,
  scrollbar,
  widget
